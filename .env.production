# Production environment variables for Docusaurus build
# Note: These are build-time variables, not runtime variables

# For GitHub Pages deployment, you need to set the correct API URL
# This should be changed to your actual deployed backend URL
REACT_APP_API_BASE_URL=https://your-backend-deployment-url.com/api/v1

# Base URL for GitHub Pages (this matches your docusaurus.config.js baseUrl)
PUBLIC_URL=/physicalaibook